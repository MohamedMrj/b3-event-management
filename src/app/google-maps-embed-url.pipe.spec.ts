import { GoogleMapsEmbedUrlPipe } from './google-maps-embed-url.pipe';

describe('GoogleMapsEmbedUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new GoogleMapsEmbedUrlPipe();
    expect(pipe).toBeTruthy();
  });
});

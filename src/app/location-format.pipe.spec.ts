import { LocationFormatPipe } from './location-format.pipe';

describe('LocationFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new LocationFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
